interface:
  display_name: "Agent Workflow Setup"
  short_description: "安装通用开发规范与专用子代理"
  default_prompt: "Use $setup:workflow to install the project agent workflow."
policy:
  allow_implicit_invocation: false
