interface:
  display_name: "Concise Plus Setup"
  short_description: "安装简洁、不附和的回答规范"
  default_prompt: "Use $setup:report-style to install Concise+ response rules."
policy:
  allow_implicit_invocation: false
