---
name: change-check
description: 改动检查入口：收尾时派 change-checker 子代理审查本次改动，意见谈拢后修改、再审直到通过。用于"定稿了""可以了，提交吧""发个 PR""合并吧""审一下这次的改动"等场景。
---

# 改动检查

## 收尾：改动检查

1. **派 `change-checker` 子代理审本次改动，不要自己审自己写的**：你手上的上下文会把刚写下的结构当成
   既定事实。检查项与判断标准都在它自己的定义里，你只交代「本次改了什么、需求是什么」，
   以及用户在几轮修改里定下的取舍——它看不见你们的对话，没交代的它会当成问题报回来。
2. **逐条处理意见**：认同的由你改；不认同的说明理由，与它谈不拢、或意见会推翻用户定过的取舍时，
   交给用户定，不要自己拍板。
3. **改完再派它审**，直到通过。汇报时写清审了几轮、改了什么、还有哪些意见交给了用户。

没有子代理机制的工具：自己审，但必须先完整读一遍改动涉及的每个文件全文，而非仅看 diff。
